import { CashflowScreen } from '@/components/finance/cashflow-screen';

/** `/app/finance/cashflow` — nakit akışı özeti + finansal yorumlar. */
export default function CashflowPage() {
  return <CashflowScreen />;
}
